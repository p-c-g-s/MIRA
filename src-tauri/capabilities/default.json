{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for mira windows",
  "windows": ["overlay", "toolbar"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
